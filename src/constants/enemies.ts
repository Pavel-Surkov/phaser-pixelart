export enum EnemyTypes {
  GOBLIN = 'goblin',
  SKELETON = 'skeleton',
}

export enum GoblinSprites {
  IDLE = 'goblin_sprite_idle',
  RUN = 'goblin_sprite_run',
  ATTACK = 'goblin_sprite_attack',
}

export enum SkeletonSprites {
  IDLE = 'skeleton_sprite_idle',
  RUN = 'skeleton_sprite_run',
  ATTACK = 'skeleton_sprite_attack',
}

export enum EnemyAnims {
  IDLE = 'idle',
  RUN = 'run',
  ATTACK = 'attack',
}

export enum EnemyStates {
  ALIVE = 'ALIVE',
  DEAD = 'DEAD',
  IMMOVABLE = 'IMMOVABLE',
}
